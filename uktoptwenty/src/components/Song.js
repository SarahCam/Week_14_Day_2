import React from "react";

const Song = (props) =>
        <React.Fragment>
          <a href="">{props.title}</a>
          <a href="">{props.artist}</a>
        </React.Fragment>

export default Song;
